namespace FileAccessTest
{
    /// <summary>
    /// Using this for binding the checkbox in the setting menu with the checkbox in the main application.
    /// Essentially it keeps the two components in sync.
    /// </summary>
    class OverwriteViewModel
    {
        public bool IsChecked { get; set; }
    }
}
